<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.sarl</groupId>
		<artifactId>io.sarl.plugins.core</artifactId>
		<version>0.10.0-SNAPSHOT</version>
	</parent>

	<groupId>io.sarl.lang</groupId>
	<artifactId>io.sarl.lang.core</artifactId>
	<packaging>eclipse-plugin</packaging>
	<name>Metamodel and Binary Code Tools</name>

	<build>
		<plugins>
			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>replacer</artifactId>
				<executions>
					<execution>
						<id>generate-javaversion-class</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>replace</goal>
						</goals>
						<configuration>
							<encoding>${project.build.sourceEncoding}</encoding>
							<basedir>${project.basedir}/src-templates</basedir>
							<outputDir>../src-gen</outputDir>
							<includes>
								<include>**/*.java</include>
							</includes>
							<replacements>
								<replacement>
									<token>@sarlspecificationreleaseversion@</token>
									<value>${sarl.specification.release_version}</value>
								</replacement>
								<replacement>
									<token>@sarlreleaseversion@</token>
									<value>${sarl.release_version}</value>
								</replacement>
								<replacement>
									<token>@sarlreleaseversion.osgi@</token>
									<value>${sarl.release_version.osgi}</value>
								</replacement>
								<replacement>
									<token>@sarlreleaseversion.maven@</token>
									<value>${sarl.release_version.maven}</value>
								</replacement>
								<replacement>
									<token>@sarlspecificationreleasestatus@</token>
									<value>${sarl.specification.release_status}</value>
								</replacement>
								<replacement>
									<token>@sarlisstableversion@</token>
									<value>${sarl.is_stable_version}</value>
								</replacement>
								<replacement>
									<token>@minimaljdkversion@</token>
									<value>${compiler.level}</value>
								</replacement>
								<replacement>
									<token>@maximaljdkversion@</token>
									<value>${compiler.level.max}</value>
								</replacement>
								<replacement>
									<token>@minimalxtextversion@</token>
									<value>${xtext.version.current}</value>
								</replacement>
							</replacements>
						</configuration>
					</execution>
					<execution>
						<id>prepare-eclipse-aboutbox</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>replace</goal>
						</goals>
						<configuration>
							<encoding>${project.build.sourceEncoding}</encoding>
							<file>${project.basedir}/src-templates/about.mappings</file>
							<outputFile>${project.basedir}/about.mappings</outputFile>
							<replacements>
								<replacement>
									<token>%website%</token>
									<value>${sarlUrl}</value>
								</replacement>
								<replacement>
									<token>%sarlBuildId%</token>
									<value>${buildId}</value>
								</replacement>
								<replacement>
									<token>%copyrightdate%</token>
									<value>${project.inceptionYear}-${year}</value>
								</replacement>
								<replacement>
									<token>%copyrighters%</token>
									<value>${copyrighters}</value>
								</replacement>
								<replacement>
									<token>%sarlreleaseversion%</token>
									<value>${sarl.release_version}</value>
								</replacement>
								<replacement>
									<token>%sarlspecificationreleasedate%</token>
									<value>${sarl.specification.release_date}</value>
								</replacement>
							</replacements>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<!-- ======================================= -->
	<!-- ==== Release Management === -->
	<!-- ======================================= -->
	<profiles>
		<profile>
			<id>default-profile</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-source-plugin</artifactId>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>maven-release-of-lang-core-plugin</id>
			<activation>
				<property>
					<name>publicSarlApiModuleSet</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<groupId>org.arakhne.afc.maven</groupId>
							<artifactId>tag-replacer</artifactId>
							<configuration>
								<sources>
									<source>${project.basedir}/src</source>
									<source>${project.basedir}/src-gen</source>
								</sources>
							</configuration>
						</plugin>
						<plugin>
							<groupId>org.apache.maven.plugins</groupId>
							<artifactId>maven-javadoc-plugin</artifactId>
							<configuration>
								<sourcepath>${project.build.directory}/generated-sources/java</sourcepath>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
	</profiles>

</project>
