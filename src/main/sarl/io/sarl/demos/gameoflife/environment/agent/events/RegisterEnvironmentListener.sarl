package io.sarl.demos.gameoflife.environment.^agent.events

import io.sarl.demos.gameoflife.environment.^agent.EnvironmentListener

/**
 * @author Maxime PINARD
 *
 */
event RegisterEnvironmentListener {

	val environmentListener : EnvironmentListener

	new(environmentListener : EnvironmentListener){

		this.environmentListener = environmentListener;
	}
}
